ui <- bslib::page_fillable(
  title = "ggNetView",
  bslib::card(
    bslib::card_header("ggNetView Shiny"),
    p("Application shell is ready. Core modules will be added in the next implementation tasks.")
  )
)
